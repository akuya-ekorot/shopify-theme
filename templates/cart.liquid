{% if cart.item_count > 0 %}
  <form action="/cart" method="post" novalidate>
  <section class="cart">
    <h1 class="cart-heading">Shopping Cart</h1>
    <div class="cart-table">
      <div class="cart-table-heading">
        <span class="cart-item-heading">ITEM</span>
        <span class="cart-original-price-heading">UNIT PRICE</span>
        <span class="cart-quantity-heading">QUANTITY</span>
        <span class="cart-price-heading">FINAL PRICE</span>
      </div>
      {% for item in cart.items %}
      <div class="cart-table-entry">
        <div class="cart-item">
          <a
                  href="/cart/change?line={{ forloop.index }}&amp;quantity=0"
                  class="remove-item"
                  title="Remove Item"
                  >
            <span>x</span>
          </a>
          <a class="cart-item-link" href="{{ item.url | within: collections.all }}">
          <img
               class="cart-item-image"
               src="{{ item | img_url: 'medium' }}"
               alt="{{ item.title | escape }}"
               height="100px"
               width="100px"
               />
          <div class="cart-item-name-variant-container">
            <span class="cart-item-name">{{ item.product.title }}</span>
            <span class="cart-item-variant">{{ item.variant.title }}</span>
          </div>
          </a>
        </div>
        <span class="cart-original-price">{{ item.original_line_price | money }}</span>
        <div class="cart-quantity">
          <input
                 class="cart-quantity-input"
                 type="number"
                 name="updates[]"
                 id="updates_{{ item.key }}"
                 value="{{ item.quantity }}"
                 min="0"
                 />
        </div>
        <span class="cart-price">{{ item.price | money }}</span>
      </div>
      {% endfor %}
      <div class="cart-subtotal-entry">
        <span class="cart-subtotal-name">Sub Total</span>
        <span class="cart-subtotal-price">{{ cart.total_price | money }}</span>
      </div>
    </div>
    <div class="cart-action">
      <button class="update-cart" name="update" type="submit">UPDATE CART</button>
      <button class="cart-checkout" name="checkout" type="submit">CHECKOUT</button>
    </div>
  </section>


























  <h1>cart</h1>
  <form action="/cart" method="post" novalidate>
    <table>
      <thead>
        <th colspan="2">Prod</th>
        <th>Price</th>
        <th>Qty</th>
        <th>total</th>
      </thead>
      <tbody>
        {% for item in cart.items %}
          <tr>
            <td>
              <a href="{{ item.url | within: collections.all }}">
                <img src="{{ item | img_url: 'medium' }}" alt="{{ item.title | escape }}">
              </a>
            </td>
            <td>
              <a href="{{ item.url }}">{{ item.product.title }}</a>
              {{ item.variant.title }}
              <a href="/cart/change?line={{ forloop.index }}&amp;quantity=0">remove</a>
            </td>
            <td>{{ item.price | money }}</td>
            <td>
              <input type="number" name="updates[]" id="updates_{{ item.key }}" value="{{ item.quantity }}" min="0">
            </td>
            <td>
              {% if item.original_line_price != item.line_price %}{{ item.original_line_price | money }}{% endif %}
              {{ item.line_price | money }}
              {% for discount in item.discounts %}<span>{{ discount.title }}</span>{% endfor %}
            </td>
          </tr>
        {% endfor %}
      </tbody>
    </table>
    <div>
      <p>sub total : {{ cart.total_price | money }}</p>
      <button type="submit" name="update">Update</button>
      <button type="submit" name="checkout">Checkout</button>
    </div>
  </form>
{% else %}
  <h2>cart</h2>
  Cart is empty
{% endif %}
